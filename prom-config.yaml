# Guarda esto como prom-config.yaml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'spark-pods'
    kubernetes_sd_configs:
      - role: pod
    relabel_configs:
      - source_labels: [__meta_kubernetes_namespace]
        action: keep
        regex: datastream-bigdata